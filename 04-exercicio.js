class Personagem {
    // Construtor para inicializar os atributos
    constructor(nome, idade, classe, hp, mp) {
        this.nome = nome;    
        this.idade = idade;   
        this.classe = classe; 
        this.hp = hp;         
        this.mp = mp;       
    }
}